import { createContext } from "react";

// const ConfigureContext = createContext<{

// }>({
//   mode: Mode.SURVIVAL,
//   setMode: () => undefined,
//   activeId: null,
//   setActiveId: () => undefined,
// });

// export const ConfigurePortal: React.FC = (props) => {


//   return (
//     <AppContext.Provider value={{ mode, setMode, activeId, setActiveId }}>
//       {props.children}
//     </AppContext.Provider>
//   );
// };
